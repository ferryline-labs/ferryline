# Experiment: inbound-usdt0-c-address

- Date: 2026-09-11T14:07:33.097Z
- Outcome: **BLOCKED**
- Question: Can inbound USDT0 be delivered to a C-address (contract / smart account) on Stellar, and how does the OFT interpret the 32-byte `to`?
- Verdict: Not run: needs a mainnet operator with USDT0 on an EVM chain and a Stellar smart account to target.

Generated by `experiments/inbound-usdt0-c-address.ts`. Read this file; do not re-run it to find out what happened.

## Evidence collected without spending

- SendParam.to is BytesN<32>. A G account key and a C contract id are both 32 bytes, so the bytes alone cannot say which kind the recipient is. How the Stellar OFT resolves that on receive is documented nowhere in the four upstream sources (VERIFIED.md §4.1).
- The OFTReceived event carries `to: Address` (verified/usdt0-oft.mainnet.rs), so a successful delivery to a contract would be visible on-chain as an oft_received event with a C address.
- Contrast: Circle's CctpForwarder receives the recipient as a strkey string and emits MuxedAddress, so the CCTP rail has no such ambiguity.

## Live run

- missing: FERRYLINE_NETWORK=mainnet
- missing: FERRYLINE_I_ACCEPT_MAINNET_SPEND=yes
- missing: FERRYLINE_STELLAR_SMART_ACCOUNT=C… (contract you control)
- missing: FERRYLINE_EVM_CHAIN
- missing: FERRYLINE_EVM_RPC_URL
- missing: FERRYLINE_EVM_PRIVATE_KEY
- Nothing was sent. The SDK keeps refusing C and M recipients for inbound USDT0 (UNSUPPORTED_RECIPIENT_KIND).
