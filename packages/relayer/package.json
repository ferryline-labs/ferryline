{
  "name": "ferryline-relayer",
  "version": "0.0.0",
  "private": true,
  "description": "Completes CCTP transfers Circle's own infrastructure does not auto-deliver: inbound (EVM -> Stellar, fee-bumped mint_and_forward) and outbound (Stellar -> EVM, gas-paid receiveMessage).",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --exclude '**/*.integration.test.ts'",
    "test:integration": "vitest run -c vitest.integration.config.ts",
    "start": "node dist/main.js",
    "clean": "rm -rf dist .turbo",
    "record:nonce-fixtures": "tsx scripts/record-nonce-check-fixtures.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.1.0",
    "@fastify/type-provider-zod": "^1.0.0",
    "@ferryline/core": "workspace:*",
    "@ferryline/sdk": "workspace:*",
    "@stellar/stellar-sdk": "^17.0.1",
    "fastify": "^5.12.4",
    "pg": "^8.23.0",
    "viem": "^2.56.3",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "testcontainers": "^12.1.0",
    "tsx": "^4.23.13"
  }
}
