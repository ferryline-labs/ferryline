node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo

# Compiled from packages/widget/src/style.css by packages/widget/scripts/build-style.mjs — a
# build artifact like dist/, not hand-written source. Regenerate with `pnpm --filter
# @ferryline/widget build:style` (or the widget's own `build` script, which runs it first).
packages/widget/src/generated-style.ts
.env
.env.*
!.env.example
.DS_Store

# Local-only scratch file, not part of the repo's real tracked content.
scf-application-guide.md

# Next.js
.next/
next-env.d.ts

# fumadocs-mdx codegen output (apps/docs) — regenerated by `next dev`/`next build`
.source/

# Rust / Soroban
contracts/router/target/
contracts/router/test_snapshots/
